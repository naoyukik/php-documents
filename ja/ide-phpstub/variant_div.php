/**
 * (PHP 5, PHP 7)<br/>
 * @link http://php.net/manual/en/function.variant-div.php
 * @param mixed $left [optional] <p> TODO DESCRIPTION </p>
 * @param mixed $right [optional] <p> TODO DESCRIPTION </p>
 * @return mixed Variant の除算規則
 * もし～なら
 * このようにします
 * 両者ともに文字列型・日付型・文字・論理型
 * Double 型を返す
 * 一方が文字列型で、もう一方が文字
 * 除算結果を double 型で返す
 * 一方が数値で、もう一方が文字列
 * 除算結果を double 型で返す
 * 両者ともに数値
 * 除算結果を double 型で返す
 * どちらか一方が NULL
 * NULL が返される
 * right が空の値で
 * left がそれ以外の型
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされる
 * left が空の値で
 * right がそれ以外の型
 * double 型の 0 が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_OVERFLOW
 * がスローされるVariant の除算規則
 * もし～なら
 * このようにします
 * 両者ともに文字列型・日付型・文字・論理型
 * Double 型を返す
 * 一方が文字列型で、もう一方が文字
 * 除算結果を double 型で返す
 * 一方が数値で、もう一方が文字列
 * 除算結果を double 型で返す
 * 両者ともに数値
 * 除算結果を double 型で返す
 * どちらか一方が NULL
 * NULL が返される
 * right が空の値で
 * left がそれ以外の型
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされる
 * left が空の値で
 * right がそれ以外の型
 * double 型の 0 が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_OVERFLOW
 * がスローされるもし～なら
 * このようにします
 * 両者ともに文字列型・日付型・文字・論理型
 * Double 型を返す
 * 一方が文字列型で、もう一方が文字
 * 除算結果を double 型で返す
 * 一方が数値で、もう一方が文字列
 * 除算結果を double 型で返す
 * 両者ともに数値
 * 除算結果を double 型で返す
 * どちらか一方が NULL
 * NULL が返される
 * right が空の値で
 * left がそれ以外の型
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされる
 * left が空の値で
 * right がそれ以外の型
 * double 型の 0 が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_OVERFLOW
 * がスローされるもし～なら
 * このようにしますもし～なら
 * このようにしますもし～ならこのようにします両者ともに文字列型・日付型・文字・論理型
 * Double 型を返す
 * 一方が文字列型で、もう一方が文字
 * 除算結果を double 型で返す
 * 一方が数値で、もう一方が文字列
 * 除算結果を double 型で返す
 * 両者ともに数値
 * 除算結果を double 型で返す
 * どちらか一方が NULL
 * NULL が返される
 * right が空の値で
 * left がそれ以外の型
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされる
 * left が空の値で
 * right がそれ以外の型
 * double 型の 0 が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_OVERFLOW
 * がスローされる両者ともに文字列型・日付型・文字・論理型
 * Double 型を返す両者ともに文字列型・日付型・文字・論理型Double 型を返す一方が文字列型で、もう一方が文字
 * 除算結果を double 型で返す一方が文字列型で、もう一方が文字除算結果を double 型で返す一方が数値で、もう一方が文字列
 * 除算結果を double 型で返す一方が数値で、もう一方が文字列除算結果を double 型で返す両者ともに数値
 * 除算結果を double 型で返す両者ともに数値除算結果を double 型で返すどちらか一方が NULL
 * NULL が返されるどちらか一方が NULLNULL が返されるright が空の値で
 * left がそれ以外の型
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされるright が空の値で
 * left がそれ以外の型rightleftcom_exception コード DISP_E_DIVBYZERO
 * がスローされるDISP_E_DIVBYZEROleft が空の値で
 * right がそれ以外の型
 * double 型の 0 が返されるleft が空の値で
 * right がそれ以外の型leftrightdouble 型の 0 が返される両者ともに空の値
 * com_exception コード DISP_E_OVERFLOW
 * がスローされる両者ともに空の値com_exception コード DISP_E_OVERFLOW
 * がスローされるDISP_E_OVERFLOW
 */
function variant_div($left, $right) {}