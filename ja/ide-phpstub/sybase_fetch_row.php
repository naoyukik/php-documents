/**
 * (PHP 4, PHP 5)<br/>
 * @link http://php.net/manual/en/function.sybase-fetch-row.php
 * @param resource $result [optional] <p> TODO DESCRIPTION </p>
 * @return array 取り込まれた行に対応する配列を返します。もう行がない場合は
 * FALSE を返します。
 * 各結果カラムは 0 から始まる配列オフセットに保持されます。FALSEデータ型
 * PHP
 * Sybase
 * string
 * VARCHAR, TEXT, CHAR, IMAGE, BINARY, VARBINARY, DATETIME
 * int
 * NUMERIC（小数点以下なし）, DECIMAL（小数点以下なし）, INT, BIT, TINYINT, SMALLINT
 * float
 * NUMERIC（小数点以下あり）, DECIMAL（小数点以下あり）, REAL, FLOAT, MONEY
 * NULL
 * NULLPHP
 * Sybase
 * string
 * VARCHAR, TEXT, CHAR, IMAGE, BINARY, VARBINARY, DATETIME
 * int
 * NUMERIC（小数点以下なし）, DECIMAL（小数点以下なし）, INT, BIT, TINYINT, SMALLINT
 * float
 * NUMERIC（小数点以下あり）, DECIMAL（小数点以下あり）, REAL, FLOAT, MONEY
 * NULL
 * NULLPHP
 * SybasePHP
 * SybasePHPSybasestring
 * VARCHAR, TEXT, CHAR, IMAGE, BINARY, VARBINARY, DATETIME
 * int
 * NUMERIC（小数点以下なし）, DECIMAL（小数点以下なし）, INT, BIT, TINYINT, SMALLINT
 * float
 * NUMERIC（小数点以下あり）, DECIMAL（小数点以下あり）, REAL, FLOAT, MONEY
 * NULL
 * NULLstring
 * VARCHAR, TEXT, CHAR, IMAGE, BINARY, VARBINARY, DATETIMEstringVARCHAR, TEXT, CHAR, IMAGE, BINARY, VARBINARY, DATETIMEint
 * NUMERIC（小数点以下なし）, DECIMAL（小数点以下なし）, INT, BIT, TINYINT, SMALLINTintNUMERIC（小数点以下なし）, DECIMAL（小数点以下なし）, INT, BIT, TINYINT, SMALLINTfloat
 * NUMERIC（小数点以下あり）, DECIMAL（小数点以下あり）, REAL, FLOAT, MONEYfloatNUMERIC（小数点以下あり）, DECIMAL（小数点以下あり）, REAL, FLOAT, MONEYNULL
 * NULLNULLNULLNULL
 */
function sybase_fetch_row($result) {}