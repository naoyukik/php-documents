/**
 * (PECL mailparse >= 0.9.0)<br/>
 * @link http://php.net/manual/en/function.mailparse-rfc822-parse-addresses.php
 * @param string $addresses [optional] <p> TODO DESCRIPTION </p>
 * @return array 各受信者について以下のキーをもつ連想配列の配列を返します。
 * display
 * 表示用の受信者名。この部分が設定されていない場合は、
 * address と同じ値となります。
 * address
 * メールアドレス。
 * is_group
 * 受信者がニュースグループである場合に TRUE、そうでない場合に FALSE。display
 * 表示用の受信者名。この部分が設定されていない場合は、
 * address と同じ値となります。
 * address
 * メールアドレス。
 * is_group
 * 受信者がニュースグループである場合に TRUE、そうでない場合に FALSE。display
 * 表示用の受信者名。この部分が設定されていない場合は、
 * address と同じ値となります。
 * address
 * メールアドレス。
 * is_group
 * 受信者がニュースグループである場合に TRUE、そうでない場合に FALSE。display
 * 表示用の受信者名。この部分が設定されていない場合は、
 * address と同じ値となります。
 * address
 * メールアドレス。
 * is_group
 * 受信者がニュースグループである場合に TRUE、そうでない場合に FALSE。display
 * 表示用の受信者名。この部分が設定されていない場合は、
 * address と同じ値となります。displaydisplay表示用の受信者名。この部分が設定されていない場合は、
 * address と同じ値となります。addressaddress
 * メールアドレス。addressaddressメールアドレス。is_group
 * 受信者がニュースグループである場合に TRUE、そうでない場合に FALSE。is_groupis_group受信者がニュースグループである場合に TRUE、そうでない場合に FALSE。TRUEFALSE
 */
function mailparse_rfc822_parse_addresses($addresses) {}