/**
 * (PECL ibm_db2 >= 1.0.0)<br/>
 * @link http://php.net/manual/en/function.db2-tables.php
 * @param resource $connection [optional] <p> TODO DESCRIPTION </p>
 * @param string $qualifier [optional] <p> TODO DESCRIPTION </p>
 * @param string $schema [optional] <p> TODO DESCRIPTION </p>
 * @param string $table-name [optional] <p> TODO DESCRIPTION </p>
 * @param string $table-type [optional] <p> TODO DESCRIPTION </p>
 * @return resource 指定したパラメータに一致するテーブルの情報を含む結果セットのリソースを返します。
 * 行の内容は、以下のカラムで構成されています。
 * カラム名
 * 説明
 * TABLE_CAT
 * テーブルを含むカタログ。テーブルがカタログを保持していない場合は NULL。
 * TABLE_SCHEM
 * テーブルを含むスキーマの名前。
 * TABLE_NAME
 * テーブルの名前。
 * TABLE_TYPE
 * テーブルの型識別子。
 * REMARKS
 * テーブルの説明。カラム名
 * 説明
 * TABLE_CAT
 * テーブルを含むカタログ。テーブルがカタログを保持していない場合は NULL。
 * TABLE_SCHEM
 * テーブルを含むスキーマの名前。
 * TABLE_NAME
 * テーブルの名前。
 * TABLE_TYPE
 * テーブルの型識別子。
 * REMARKS
 * テーブルの説明。カラム名
 * 説明
 * TABLE_CAT
 * テーブルを含むカタログ。テーブルがカタログを保持していない場合は NULL。
 * TABLE_SCHEM
 * テーブルを含むスキーマの名前。
 * TABLE_NAME
 * テーブルの名前。
 * TABLE_TYPE
 * テーブルの型識別子。
 * REMARKS
 * テーブルの説明。カラム名
 * 説明カラム名
 * 説明カラム名説明TABLE_CAT
 * テーブルを含むカタログ。テーブルがカタログを保持していない場合は NULL。
 * TABLE_SCHEM
 * テーブルを含むスキーマの名前。
 * TABLE_NAME
 * テーブルの名前。
 * TABLE_TYPE
 * テーブルの型識別子。
 * REMARKS
 * テーブルの説明。TABLE_CAT
 * テーブルを含むカタログ。テーブルがカタログを保持していない場合は NULL。TABLE_CATテーブルを含むカタログ。テーブルがカタログを保持していない場合は NULL。NULLTABLE_SCHEM
 * テーブルを含むスキーマの名前。TABLE_SCHEMテーブルを含むスキーマの名前。TABLE_NAME
 * テーブルの名前。TABLE_NAMEテーブルの名前。TABLE_TYPE
 * テーブルの型識別子。TABLE_TYPEテーブルの型識別子。REMARKS
 * テーブルの説明。REMARKSテーブルの説明。
 */
function db2_tables($connection, $qualifier, $schema, $table-name, $table-type) {}