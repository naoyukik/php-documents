/**
 * (PECL apc >= 3.1.4)<br/>
 * @link http://php.net/manual/en/function.apc-exists.php
 * @param mixed $keys [optional] <p> TODO DESCRIPTION </p>
 * @return mixed キーが存在する場合に TRUE、それ以外の場合に FALSE を返します。
 * keys に配列を渡したときは、
 * 存在するキーをすべて含む配列を返します。
 * どれも存在しない場合は空の配列を返します。TRUEFALSEkeys
 */
function apc_exists($keys) {}