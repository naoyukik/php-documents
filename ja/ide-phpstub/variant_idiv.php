/**
 * (PHP 5, PHP 7)<br/>
 * @link http://php.net/manual/en/function.variant-idiv.php
 * @param mixed $left [optional] <p> TODO DESCRIPTION </p>
 * @param mixed $right [optional] <p> TODO DESCRIPTION </p>
 * @return mixed Variant の整数除算規則
 * もし～なら
 * このようにします
 * 両者ともに文字列型・日付型・文字・論理型
 * 除算結果を integer 型で返す
 * 一方が文字列型で、もう一方が文字
 * 除算
 * 一方が数値で、もう一方が文字列
 * 除算
 * 両者ともに数値
 * 除算
 * どちらか一方が NULL
 * NULL が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされるVariant の整数除算規則
 * もし～なら
 * このようにします
 * 両者ともに文字列型・日付型・文字・論理型
 * 除算結果を integer 型で返す
 * 一方が文字列型で、もう一方が文字
 * 除算
 * 一方が数値で、もう一方が文字列
 * 除算
 * 両者ともに数値
 * 除算
 * どちらか一方が NULL
 * NULL が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされるもし～なら
 * このようにします
 * 両者ともに文字列型・日付型・文字・論理型
 * 除算結果を integer 型で返す
 * 一方が文字列型で、もう一方が文字
 * 除算
 * 一方が数値で、もう一方が文字列
 * 除算
 * 両者ともに数値
 * 除算
 * どちらか一方が NULL
 * NULL が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされるもし～なら
 * このようにしますもし～なら
 * このようにしますもし～ならこのようにします両者ともに文字列型・日付型・文字・論理型
 * 除算結果を integer 型で返す
 * 一方が文字列型で、もう一方が文字
 * 除算
 * 一方が数値で、もう一方が文字列
 * 除算
 * 両者ともに数値
 * 除算
 * どちらか一方が NULL
 * NULL が返される
 * 両者ともに空の値
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされる両者ともに文字列型・日付型・文字・論理型
 * 除算結果を integer 型で返す両者ともに文字列型・日付型・文字・論理型除算結果を integer 型で返す一方が文字列型で、もう一方が文字
 * 除算一方が文字列型で、もう一方が文字除算一方が数値で、もう一方が文字列
 * 除算一方が数値で、もう一方が文字列除算両者ともに数値
 * 除算両者ともに数値除算どちらか一方が NULL
 * NULL が返されるどちらか一方が NULLNULL が返される両者ともに空の値
 * com_exception コード DISP_E_DIVBYZERO
 * がスローされる両者ともに空の値com_exception コード DISP_E_DIVBYZERO
 * がスローされるDISP_E_DIVBYZERO
 */
function variant_idiv($left, $right) {}