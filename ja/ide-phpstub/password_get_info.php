/**
 * (PHP 5 >= 5.5.0, PHP 7)<br/>
 * @link http://php.net/manual/en/function.password-get-info.php
 * @param string $hash [optional] <p> TODO DESCRIPTION </p>
 * @return array 三つの要素を持つ連想配列を返します。
 * algo には、そのハッシュの
 * パスワードアルゴリズム定数
 * が含まれます。
 * algoName には、そのアルゴリズムの名前が含まれます。
 * options には、
 * password_hash を呼んだときのオプションが含まれます。algo には、そのハッシュの
 * パスワードアルゴリズム定数
 * が含まれます。
 * algoName には、そのアルゴリズムの名前が含まれます。
 * options には、
 * password_hash を呼んだときのオプションが含まれます。algo には、そのハッシュの
 * パスワードアルゴリズム定数
 * が含まれます。algo には、そのハッシュの
 * パスワードアルゴリズム定数
 * が含まれます。algoパスワードアルゴリズム定数algoName には、そのアルゴリズムの名前が含まれます。algoName には、そのアルゴリズムの名前が含まれます。algoNameoptions には、
 * password_hash を呼んだときのオプションが含まれます。options には、
 * password_hash を呼んだときのオプションが含まれます。optionspassword_hash
 */
function password_get_info($hash) {}