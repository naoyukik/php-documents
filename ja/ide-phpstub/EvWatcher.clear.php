/**
 * (PECL ev >= 0.2.0)<br/>
 * @link http://php.net/manual/en/evwatcher.clear.php
 * @return int In case if the watcher is pending, returns
 * revents
 * bitset as if the
 * watcher callback
 * had been invoked. Otherwise returns
 * 0
 * .reventswatcher callback0
 */
function EvWatcher.clear() {}