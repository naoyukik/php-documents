/**
 * (PHP 4 >= 4.0.6, PHP 5, PHP 7)<br/>
 * @link http://php.net/manual/en/function.fbsql-fetch-field.php
 * @param resource $result [optional] <p> TODO DESCRIPTION </p>
 * @param int $field_offset [optional] <p> TODO DESCRIPTION </p>
 * @return object フィールド情報を含むオブジェクト、あるいはエラー時に FALSE を返します。FALSEオブジェクトのプロパティは以下のとおりです。
 * name - カラム名。
 * table - カラムが属するテーブルの名前。
 * max_length - カラムの最大長。
 * not_null - カラムが NULL にならない場合に 1。
 * type - カラムの型。name - カラム名。
 * table - カラムが属するテーブルの名前。
 * max_length - カラムの最大長。
 * not_null - カラムが NULL にならない場合に 1。
 * type - カラムの型。name - カラム名。name - カラム名。table - カラムが属するテーブルの名前。table - カラムが属するテーブルの名前。max_length - カラムの最大長。max_length - カラムの最大長。not_null - カラムが NULL にならない場合に 1。not_null - カラムが NULL にならない場合に 1。NULLtype - カラムの型。type - カラムの型。
 */
function fbsql_fetch_field($result, $field_offset) {}