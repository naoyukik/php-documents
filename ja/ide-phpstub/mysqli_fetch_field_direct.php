/**
 * (PHP 5, PHP 7)<br/>
 * @link http://php.net/manual/en/mysqli-result.fetch-field-direct.php
 * @param int $fieldnr [optional] <p> TODO DESCRIPTION </p>
 * @param mysqli_result $result [optional] <p> TODO DESCRIPTION </p>
 * @return object フィールド定義情報を含むオブジェクトを返します。もし、指定した
 * fieldnr のフィールドの情報が取得できない場合は
 * FALSE を返します。fieldnrFALSEオブジェクトの属性
 * 属性
 * 説明
 * name
 * カラムの名前。
 * orgname
 * もしエイリアスが指定されている場合の、本来の名前。
 * table
 * フィールドが属するテーブルの名前。
 * orgtable
 * もしエイリアスが指定されている場合の、本来のテーブル名。
 * def
 * フィールドのデフォルト値。文字列形式。
 * max_length
 * 結果セットにおけるフィールドの最大幅。
 * length
 * テーブルの定義で指定されているフィールド幅。
 * charsetnr
 * フィールドの文字セット番号。
 * flags
 * フィールドのビットフラグを整数型で表す。
 * type
 * フィールドのデータ型。
 * decimals
 * フィールドの桁数（数値型のフィールド）。オブジェクトの属性
 * 属性
 * 説明
 * name
 * カラムの名前。
 * orgname
 * もしエイリアスが指定されている場合の、本来の名前。
 * table
 * フィールドが属するテーブルの名前。
 * orgtable
 * もしエイリアスが指定されている場合の、本来のテーブル名。
 * def
 * フィールドのデフォルト値。文字列形式。
 * max_length
 * 結果セットにおけるフィールドの最大幅。
 * length
 * テーブルの定義で指定されているフィールド幅。
 * charsetnr
 * フィールドの文字セット番号。
 * flags
 * フィールドのビットフラグを整数型で表す。
 * type
 * フィールドのデータ型。
 * decimals
 * フィールドの桁数（数値型のフィールド）。属性
 * 説明
 * name
 * カラムの名前。
 * orgname
 * もしエイリアスが指定されている場合の、本来の名前。
 * table
 * フィールドが属するテーブルの名前。
 * orgtable
 * もしエイリアスが指定されている場合の、本来のテーブル名。
 * def
 * フィールドのデフォルト値。文字列形式。
 * max_length
 * 結果セットにおけるフィールドの最大幅。
 * length
 * テーブルの定義で指定されているフィールド幅。
 * charsetnr
 * フィールドの文字セット番号。
 * flags
 * フィールドのビットフラグを整数型で表す。
 * type
 * フィールドのデータ型。
 * decimals
 * フィールドの桁数（数値型のフィールド）。属性
 * 説明属性
 * 説明属性説明name
 * カラムの名前。
 * orgname
 * もしエイリアスが指定されている場合の、本来の名前。
 * table
 * フィールドが属するテーブルの名前。
 * orgtable
 * もしエイリアスが指定されている場合の、本来のテーブル名。
 * def
 * フィールドのデフォルト値。文字列形式。
 * max_length
 * 結果セットにおけるフィールドの最大幅。
 * length
 * テーブルの定義で指定されているフィールド幅。
 * charsetnr
 * フィールドの文字セット番号。
 * flags
 * フィールドのビットフラグを整数型で表す。
 * type
 * フィールドのデータ型。
 * decimals
 * フィールドの桁数（数値型のフィールド）。name
 * カラムの名前。nameカラムの名前。orgname
 * もしエイリアスが指定されている場合の、本来の名前。orgnameもしエイリアスが指定されている場合の、本来の名前。table
 * フィールドが属するテーブルの名前。tableフィールドが属するテーブルの名前。orgtable
 * もしエイリアスが指定されている場合の、本来のテーブル名。orgtableもしエイリアスが指定されている場合の、本来のテーブル名。def
 * フィールドのデフォルト値。文字列形式。defフィールドのデフォルト値。文字列形式。max_length
 * 結果セットにおけるフィールドの最大幅。max_length結果セットにおけるフィールドの最大幅。length
 * テーブルの定義で指定されているフィールド幅。lengthテーブルの定義で指定されているフィールド幅。charsetnr
 * フィールドの文字セット番号。charsetnrフィールドの文字セット番号。flags
 * フィールドのビットフラグを整数型で表す。flagsフィールドのビットフラグを整数型で表す。type
 * フィールドのデータ型。typeフィールドのデータ型。decimals
 * フィールドの桁数（数値型のフィールド）。decimalsフィールドの桁数（数値型のフィールド）。
 */
function mysqli_fetch_field_direct($fieldnr, $result) {}