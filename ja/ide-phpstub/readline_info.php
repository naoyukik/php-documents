/**
 * (PHP 4, PHP 5, PHP 7)<br/>
 * @link http://php.net/manual/en/function.readline-info.php
 * @param string $varname [optional] <p> TODO DESCRIPTION </p>
 * @param string $newvalue [optional] <p> TODO DESCRIPTION </p>
 * @return mixed パラメータを指定しないでコールした場合、この関数は readline が使用する
 * すべての設定の値を配列で返します。要素の添字は次のようになります。
 * done, end, erase_empty_line, library_version, 
 * line_buffer, mark, pending_input, point, prompt, readline_name,
 * terminal_nameひとつあるいはふたつのパラメータを指定してコールした場合は、
 * 元の値が返されます。
 */
function readline_info($varname, $newvalue) {}