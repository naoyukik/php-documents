/**
 * (PHP 4, PHP 5)<br/>
 * @link http://php.net/manual/en/function.sybase-fetch-array.php
 * @param resource $result [optional] <p> TODO DESCRIPTION </p>
 * @return array 取り込んだ行を表す配列を返します。もう行がない場合には FALSE
 * を返します。FALSE同じ名前のフィールドを選択する場合（たとえば join を行った場合など）、
 * 連想インデックスは順に番号を付加します。
 * 詳細は例を参照ください。同じ名前のフィールドを選択する場合（たとえば join を行った場合など）、
 * 連想インデックスは順に番号を付加します。
 * 詳細は例を参照ください。
 */
function sybase_fetch_array($result) {}