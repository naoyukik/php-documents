/**
 * (PECL pthreads >= 2.0.0)<br/>
 * @link http://php.net/manual/en/pool.submit.php
 * @param Threaded $task [optional] <p> TODO DESCRIPTION </p>
 * @return int the identifier of the Worker executing the object
 */
function Pool.submit($task) {}