/**
 * (PECL CUBRID >= 8.3.0)<br/>
 * @link http://php.net/manual/en/function.cubrid-num-rows.php
 * @param resource $result [optional] <p> TODO DESCRIPTION </p>
 * @return int Number of rows, when process is successful.0 when the query was done in async mode.-1, if SQL statement is not SELECT.FALSE when process is unsuccessful.FALSE
 */
function cubrid_num_rows($result) {}