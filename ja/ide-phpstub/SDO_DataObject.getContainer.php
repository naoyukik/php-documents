/**
 * (^)<br/>
 * @link http://php.net/manual/en/sdo-dataobject.getcontainer.php
 * @return SDO_DataObject この SDO_DataObject を保有している SDO_DataObject、あるいは
 * もしこのオブジェクトが階層のルートである場合
 * (つまり、コンテナが存在しない場合) は NULL を返します。
 */
function SDO_DataObject.getContainer() {}