/**
 * (PECL sam >= 0.1.0)<br/>
 * @link http://php.net/manual/en/samconnection.subscribe.php
 * @param string $targetTopic [optional] <p> TODO DESCRIPTION </p>
 * @return string 購読の識別子を返します。これを、それ以降にデータを取得する際に
 * トピックのデータを取得するためのセレクタとして使用します。
 * エラーが発生した場合には FALSE を返します。
 * この識別子を使用して、特定のトピック名の受信コールを行います。FALSE
 */
function SAMConnection.subscribe($targetTopic) {}