/**
 * (PHP 7)<br/>
 * @link http://php.net/manual/en/intlchar.tolower.php
 * @param mixed $codepoint [optional] <p> TODO DESCRIPTION </p>
 * @return mixed Returns the Simple_Lowercase_Mapping of the code point, if any;
 * otherwise the code point itself.戻り値の型は integer になります。ただし、コードポイントを UTF-8 文字列で渡した場合は別で、その場合の返り値の型は文字列になります。integer
 */
function IntlChar.tolower($codepoint) {}