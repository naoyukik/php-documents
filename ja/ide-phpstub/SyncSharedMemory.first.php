/**
 * (PECL sync >= 1.1.0)<br/>
 * @link http://php.net/manual/en/syncsharedmemory.first.php
 * @return bool A boolean of TRUE if the object is the first instance system-wide, FALSE otherwise.
 */
function SyncSharedMemory.first() {}