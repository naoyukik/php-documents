/**
 * (PECL zmq >= 0.5.0)<br/>
 * @link http://php.net/manual/en/zmqcontext.ispersistent.php
 * @return boolean Returns TRUE if the context is persistent and FALSE if the context is non-persistent.TRUEFALSE
 */
function ZMQContext.isPersistent() {}